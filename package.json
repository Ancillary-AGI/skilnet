@@ .. @@
 {
   "name": "eduverse",
-  "version": "1.0.0",
+  "version": "2.0.0",
   "description": "Advanced E-Learning Platform with VR/AR and AI capabilities",
   "main": "index.js",
   "scripts": {
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "test": "echo \"Error: no test specified\" && exit 1",
+    "dev": "cd frontend && flutter run -d web-server --web-port 3000",
+    "backend": "cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
+    "docker:up": "docker-compose up -d",
+    "docker:down": "docker-compose down",
+    "test:backend": "cd backend && pytest",
+    "test:frontend": "cd frontend && flutter test",
+    "test:integration": "cd frontend && flutter test integration_test/",
+    "build:frontend": "cd frontend && flutter build web",
+    "deploy": "docker-compose -f docker-compose.prod.yml up -d"
   },
   "keywords": [
-    "e-learning",
-    "education",
-    "platform"
+    "e-learning", "education", "platform", "vr", "ar", "ai", "global", "accessibility", "mental-health"
   ],
   "author": "Victor Anya",
   "license": "MIT",
   "devDependencies": {
     "@types/node": "^20.10.0",
     "typescript": "^5.3.0",
     "ts-node": "^10.9.0"
   }
 }